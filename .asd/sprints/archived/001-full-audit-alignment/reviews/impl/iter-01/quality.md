[REVIEW-impl-quality]: CONCERNS

# Review — quality

- **Phase**: impl-review
- **Iteration**: 1

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| 1 | medium | `Source/EquipmentManager/EquipmentManagerMapComponent.cs:388-389`, `:513-514` | AC-19 (C-12) is only partially implemented. `UpdatePrimaryWeapons` (line 490) correctly builds `assignedByOthers` once and mutates it incrementally, but `UpdateMeleeSidearms` and `UpdateRangedSidearms` still rebuild the full set via `_pawnCache.Values.Where(pc => pc != pawn).SelectMany(pc => pc.AssignedWeapons.Keys)` inside the per-pawn `foreach` — the exact O(pawns²×weapons) per-pawn allocation/scan AC-19 says must be gone. The incremental `assignedByOthers.Add(...)` calls inside these loops (lines 418/441 and the ranged equivalents) are defeated by the per-iteration rebuild. | Build `assignedByOthers` once before the pawn loop (from all pawns), keep mutating it incrementally with `.Add(...)` as weapons are assigned. The `pc != pawn` exclusion is unnecessary for correctness here since a pawn's own already-assigned weapons should not be re-handed to it anyway, and the per-pawn `pawn.AssignedWeapons.Keys.All(...)` def-check already prevents self-collision. |
| 2 | medium | `Source/EquipmentManager.Tests/ItemRuleAndLoadoutTests.cs:68,81,131,143,154`; `WorkTypeThingRuleTests.cs:48` | Multiple `[Test]` methods contain zero assertions — only comments ("Verified by direct code inspection", "documented only"). They pass unconditionally and report false coverage for AC-26/AC-27/AC-28/AC-29. `WorkTypeThingRule_ToolRuleIntegration_ConsumesWorkTypeStatMap` and all five listed Loadout tests assert nothing. | Either implement real assertions (the listed Loadout setter/Initialize behaviors are unit-testable on a `Loadout` instance without full game context, mirroring the existing `ItemRule_Initialize_*` tests that already exercise `RangedWeaponRule`), or delete the empty test bodies so they do not masquerade as coverage. Empty `[Test]` shells are worse than no test. (Testing reviewer owns depth; flagged here as an obvious test defect.) |
| 3 | medium | `Source/EquipmentManager.Tests/ItemRuleAndLoadoutTests.cs:167-173` | AC-29 explicitly requires a passing test "asserting the C-3 fix" (tool-cache composite-key correctness; C-3/AC-15 is a must-priority defect). The implementation reduced this to a comment block with no executable test — there is NO test asserting that differing work-type sets yield differing `ToolCache.GetStatValue` scores. The headline defect fix is uncharacterized. | Add a test exercising `ToolCache.GetStatValue(stat, workTypesA)` vs `GetStatValue(stat, workTypesB)` for a WorkType-dependent custom stat and assert the scores differ when the work-type sets differ. If a live `Thing`/cache cannot be built in isolation, extract the work-type-averaging logic into a testable seam. |
| 4 | medium | `Source/EquipmentManager.Tests/ItemRuleAndLoadoutTests.cs:11-12,18,40,64,77,90,103,127,139,150,160,167,170`; `WorkTypeThingRuleTests.cs:8,11,18,33`; `StatRangesTests.cs:8,37,52` | Test code is littered with ASD-artifact references (`AC-27`, `AC-29`, `C-3`, `C-4`, `OQ-1`, and `Task 7` at `ItemRuleAndLoadoutTests.cs:170`). The self-contained-code rule forbids code AND tests from referencing ASD artifacts (it names `AC-N` and `Task N` specifically); the corpus must be self-sufficient without the design docs. | Rewrite these comments to explain the *why* directly (e.g. "first observed value must seed the range to `[v,v]`, not `[0,v]`") with no `AC-`/`C-`/`OQ-`/`Task` identifiers. Forward-looking `TODO`/`FIXME` referencing a future sprint is the only allowed exception, which none of these are. |
| 5 | low | `Source/EquipmentManager/EquipmentManagerMapComponent.cs:381,485` | The player-facing in-memory log buffer (`EquipmentManagerGameComponent.LogMessage`) is fed hardcoded `"Equipment Manager: "`-prefixed strings. This is the user-visible LogDialog feature, not the diagnostic `Logger` wrapper, so AC-21's prefix-drop does not strictly apply — but the literal prefix is redundant inside the mod's own log dialog and the surrounding text is not routed through `Resources.Strings`. Low severity: dev/diagnostic buffer, not localized gameplay UI. | Drop the redundant `"Equipment Manager: "` prefix from the buffer messages (the dialog already scopes them to the mod); leave keying as-is since this is a diagnostic buffer, not player UI text. |

## Verdict
CONCERNS: 5

## Next action
Route back to `impl` (fix mode). The responsible dev resolves findings #1–#5, then the sprint re-enters impl-review. None of these require escalation: #1 is a performance pattern AC-19 already mandates; #2/#3/#4 are test-quality/standards fixes within the existing test infrastructure; #5 is a one-line cleanup. No new abstraction, contract change, or scope expansion is implied.

Note (no finding): production-code defect fixes verified correct — C-3 ToolCache (WorkType stats correctly bypass the StatDef-only cache and resolve via per-work-type `_workTypeScores` averaging), C-6 RangedWeaponCache (accuracy/DPSA bands zeroed at Update entry, persistent fields untouched), C-8 prune (safe two-phase collect-then-remove, no mutate-during-iterate). Nullable migration is well-guarded (`??=` restore in `Initialize()` before any `!` deref; `= null!` limited to documented Scribe/DefOf load-contract fields). CE reflection delegates remain nullable and guarded at every use site (AC-14); SimpleSidearms `WeaponAssingment` typo preserved (AC-31); CE stays reflective (AC-32). Logger routing clean — no raw `Verse.Log.*` outside `Logger.cs` (AC-21/AC-22). Consolidation deletes confirmed: no `WorkTypeRule`/`ItemCache`/`EquipmentManagerStatDefs`/`NormalizeStatRanges`/`DefaultWorkTypeStats` symbols remain (AC-2…AC-8). No JetBrains nullability attributes or nullable-warning suppressions remain (AC-11/AC-12).

## Escalations (optional)
- none
